{
  "name": "unifi-allowlist-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker front end for unifi-allowlist",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute unifi-allowlist --remote --file=./schema.sql",
    "schema:local": "wrangler d1 execute unifi-allowlist --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
